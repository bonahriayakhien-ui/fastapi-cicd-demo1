# ============================================================
# Pipeline CI/CD hoàn chỉnh: Test -> Build & Push image -> (Deploy)
# ============================================================
name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ================= JOB 1: TEST (CI) =================
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest -v

  # ============ JOB 2: BUILD & PUSH IMAGE (CD) ============
  build-and-push:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # ================= JOB 3: DEPLOY (CD) — tuỳ chọn =================
  # Bỏ comment và cấu hình 3 secrets: SERVER_HOST, SERVER_USER, SSH_PRIVATE_KEY
  # nếu bạn có VPS riêng để deploy tự động qua SSH.
  #
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Deploy to server via SSH
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USER }}
  #         key: ${{ secrets.SSH_PRIVATE_KEY }}
  #         script: |
  #           docker pull ghcr.io/${{ github.repository }}:latest
  #           docker stop myapp || true
  #           docker rm myapp || true
  #           docker run -d --name myapp --restart unless-stopped \
  #             -p 80:8000 \
  #             ghcr.io/${{ github.repository }}:latest
  #           docker image prune -f
